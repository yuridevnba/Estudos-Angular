import { MultiplicarPorPipe } from './multiplicar-por.pipe';

describe('MultiplicarPorPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplicarPorPipe();
    expect(pipe).toBeTruthy();
  });
});
